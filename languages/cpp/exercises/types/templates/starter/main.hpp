// Copyright 2026 Ryan P. Walsh (rpwalsh.github.io)
#ifndef MAIN_HPP
#define MAIN_HPP

#include <array>
#include <cstddef>
#include <string>
#include <vector>

// Define: template <typename T, std::size_t Capacity> class RingBuffer
//   push(value)  size()  full()  oldest()  dropped()  drain()
//
// Define: template <typename T> std::string describe(const T &value)
// Define: template <> std::string describe<bool>(const bool &value)
// Define: template <> std::string describe<std::string>(const std::string &value)

#endif /* MAIN_HPP */
