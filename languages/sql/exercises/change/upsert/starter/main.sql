-- Copyright 2026 Ryan P. Walsh (rpwalsh.github.io)
-- Upsert the pair ('theme', 'light'): insert it if absent, otherwise
-- update the value and add one to updates. Return key, value, updates.

SELECT 'not implemented' AS key;
