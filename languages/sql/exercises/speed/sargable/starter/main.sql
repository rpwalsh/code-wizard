-- Copyright 2026 Ryan P. Walsh (rpwalsh.github.io)
-- March 2026 orders: id, customer, placed_at, total — ordered by placed_at,
-- then id. The predicate must be sargable: a bare indexed column compared
-- against constants. No functions wrapped around placed_at.

SELECT 'not implemented' AS id;
